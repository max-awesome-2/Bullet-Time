
/**
 This interface is used by all types of object that need to be updated every frame.
 */
interface Updateable {

  /**
   The update method that will be called every frame.
   */
  void update();
}
